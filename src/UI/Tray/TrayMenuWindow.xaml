<Window
	x:Class="WindowsOscVolumeControl.UI.Tray.TrayMenuWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	AllowsTransparency="True"
	Background="Transparent"
	ResizeMode="NoResize"
	ShowActivated="False"
	ShowInTaskbar="False"
	SizeToContent="WidthAndHeight"
	Topmost="True"
	UseLayoutRounding="True"
	SnapsToDevicePixels="True"
	WindowStyle="None">
	<Window.Resources>
		<Style x:Key="TrayMenuButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
			<Setter Property="HorizontalAlignment" Value="Stretch" />
			<Setter Property="HorizontalContentAlignment" Value="Left" />
			<Setter Property="VerticalContentAlignment" Value="Center" />
			<Setter Property="Padding" Value="12,8" />
			<Setter Property="Margin" Value="0,1" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border
							x:Name="Chrome"
							Background="{TemplateBinding Background}"
							CornerRadius="6"
							SnapsToDevicePixels="True">
							<ContentPresenter
								Margin="{TemplateBinding Padding}"
								HorizontalAlignment="Stretch"
								RecognizesAccessKey="True" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource TextControlBackgroundPointerOver}" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="Chrome" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="Chrome" Property="Opacity" Value="0.55" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="TrayMenuIconStyle" TargetType="TextBlock">
			<Setter Property="FontFamily" Value="{DynamicResource SymbolThemeFontFamily}" />
			<Setter Property="FontSize" Value="16" />
			<Setter Property="Width" Value="18" />
			<Setter Property="TextAlignment" Value="Center" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Margin" Value="0,0,10,0" />
		</Style>
		<!-- The Close glyph runs visually larger than other symbols; slightly downscale to match. -->
		<Style x:Key="TrayMenuIconCloseStyle" TargetType="TextBlock" BasedOn="{StaticResource TrayMenuIconStyle}">
			<Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
			<Setter Property="RenderTransform">
				<Setter.Value>
					<ScaleTransform ScaleX="0.9" ScaleY="0.9" />
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="TrayMenuDangerButtonStyle" TargetType="Button" BasedOn="{StaticResource TrayMenuButtonStyle}">
			<Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
		</Style>
	</Window.Resources>

	<Grid Margin="6" Background="Transparent">
		<Border
			x:Name="SurfaceBackground"
			Background="{x:Null}"
			Opacity="0.9"
			CornerRadius="10"
			SnapsToDevicePixels="True">
			<Border.Effect>
				<DropShadowEffect BlurRadius="18" Opacity="0.28" ShadowDepth="2" />
			</Border.Effect>
		</Border>

		<Border
			x:Name="SurfaceBorder"
			BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="10"
			SnapsToDevicePixels="True">
			<StackPanel Margin="8" MinWidth="192">
				<TextBlock
					x:Name="EndPointText"
					Margin="8,6,8,6"
					TextAlignment="Center"
					Foreground="{DynamicResource TextControlForeground}"
					FontWeight="SemiBold"
					Text="0 . 0 . 0 . 0 : 0" />

				<Separator Margin="6,6,6,6" />

				<Button
					Style="{StaticResource TrayMenuButtonStyle}"
					Click="configure_Click"
					>
					<StackPanel Orientation="Horizontal">
						<TextBlock
							Style="{StaticResource TrayMenuIconStyle}"
							Text="&#xE713;" />
						<TextBlock Text="Settings ..." />
					</StackPanel>
				</Button>
				<Button
					Style="{StaticResource TrayMenuDangerButtonStyle}"
					Click="exit_Click"
					>
					<StackPanel Orientation="Horizontal">
						<TextBlock
							Style="{StaticResource TrayMenuIconCloseStyle}"
							Text="&#xE8BB;" />
						<TextBlock Text="Exit" />
					</StackPanel>
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</Window>

