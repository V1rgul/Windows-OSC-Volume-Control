<Window
    x:Class="WindowsOscVolumeControl.OSDController"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="OSD"
    Width="420"
    Height="78"
    AllowsTransparency="True"
    Background="Transparent"
    ShowInTaskbar="False"
    ShowActivated="False"
    Topmost="True"
    WindowStyle="None"
    WindowStartupLocation="Manual"
    ResizeMode="NoResize">
    <Border
        x:Name="RootBorder"
        Background="#D9000000"
        BorderBrush="#26000000"
        BorderThickness="1"
        CornerRadius="0"
        Padding="0">
        <DockPanel LastChildFill="False">
            <StackPanel
                x:Name="RightContent"
                DockPanel.Dock="Right"
                Orientation="Horizontal"
                VerticalAlignment="Top">
                <TextBlock
                    x:Name="ValueTextBlock"
                    MinWidth="0"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="White"
                    TextAlignment="Right"
                    VerticalAlignment="Top"
                    Text="0.00" />
                <Grid
                    x:Name="ToggleIconContainer"
                    Width="0"
                    Height="0"
                    Margin="0"
                    Visibility="Collapsed">
                    <Ellipse
                        x:Name="ToggleEllipse"
                        StrokeThickness="2" />
                    <Line
                        x:Name="ToggleSlash"
                        X1="4"
                        Y1="14"
                        X2="14"
                        Y2="4"
                        StrokeThickness="2"
                        StrokeStartLineCap="Round"
                        StrokeEndLineCap="Round"
                        Visibility="Collapsed" />
                </Grid>
            </StackPanel>

            <Grid
                x:Name="MiddleContent"
                DockPanel.Dock="Right"
                Margin="0"
                VerticalAlignment="Top">
                <Border
                    x:Name="BarTrack"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="#33FFFFFF">
                    <Grid ClipToBounds="True">
                        <Border
                            x:Name="BarFill"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="#7AD77A"
                            CornerRadius="10,0,0,10"
                            Width="0" />
                        <Grid
                            x:Name="FlashMarker"
                            Width="0"
                            Height="0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Visibility="Collapsed"
                            Panel.ZIndex="2">
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="FlashTransform" />
                            </Grid.RenderTransform>
                            <Border
                                x:Name="FlashMarkerHorizontal"
                                Width="0"
                                Height="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Background="Black"
                                CornerRadius="1.5" />
                            <Border
                                x:Name="FlashMarkerVertical"
                                Width="0"
                                Height="0"
                                Margin="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Background="Black"
                                CornerRadius="1.5"
                                Visibility="Collapsed" />
                        </Grid>
                    </Grid>
                </Border>
                <TextBlock
                    x:Name="StatusTextBlock"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Text="Pending"
                    Visibility="Collapsed" />
            </Grid>

            <TextBlock
                x:Name="LabelTextBlock"
                Margin="0"
                VerticalAlignment="Top"
                FontSize="12"
                FontWeight="SemiBold"
                Foreground="White"
                TextTrimming="CharacterEllipsis"
                Visibility="Collapsed" />
        </DockPanel>
    </Border>
</Window>
